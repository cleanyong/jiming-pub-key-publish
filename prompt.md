

在這個RUST Project裡頭,我們今天要建立一個應用,就是你幫忙生成一個應用,就是一個網站吧,功能呢就是可以讓用戶來在這個網站那裡發佈他們的用於簽名的public key,一行吧,輸入就是一行。 就是因為我推薦使用ED25519的那個,就是EDDSC的那個簽名的key,是比較短的,當然還有另外一個fuel是一個option的,就是他們可以寫點注釋在上面。 然後呢,點擊發佈了之後呢,就會給他一個鏈接,這個鏈接呢,這個key的owner呢,就可以通過各種各樣的渠道,電子郵件啊,聊天室啊,或者是網站啊,微博啊,去把這個公要發佈出去。

有几个方面需要改进一下。 第一个呢,就是对于那个输入的那个public key要进行一下验证。 它是只是base64 encoding的。 那这样的话就不能够奇奇怪怪的输入一些那些特殊的字符,只是base64。 另外一个呢,就是生成的那个链接呢,变成是一个可拷贝的。 另外呢,就是把网站的那个背景就是做成跟之前的那个就是深色调的吧。

please use sqlite database to save the data

SQLite 数据库要写一个给它序列化,往数据库里写的这个,因为它是一个单线程的数据库,就是在写的时候呢,让它序列写入,不要产生竞争关系。

Key的最長可以存儲1000個字節,注釋最長可以存儲100個字節,在數據庫裡,就是輸入有這麼一個限制吧。